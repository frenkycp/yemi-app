<?php
// This class was automatically generated by a giiant build task
// You should not change it manually as it will be overwritten on next build

namespace app\controllers\base;

use app\models\SapPoRcv;
    use app\models\search\SapPoRcvSearch;
use yii\web\Controller;
use yii\web\HttpException;
use yii\helpers\Url;
use yii\filters\AccessControl;
use dmstr\bootstrap\Tabs;

/**
* SapPoRcvController implements the CRUD actions for SapPoRcv model.
*/
class SapPoRcvController extends Controller
{


/**
* @var boolean whether to enable CSRF validation for the actions in this controller.
* CSRF validation is enabled only when both this property and [[Request::enableCsrfValidation]] are true.
*/
public $enableCsrfValidation = false;


/**
* Lists all SapPoRcv models.
* @return mixed
*/
public function actionIndex()
{
    $searchModel  = new SapPoRcvSearch;
    $searchModel->rcv_date = date('Y-m-d');

    if(\Yii::$app->request->get('rcv_date') !== null)
    {
    	$searchModel->rcv_date = \Yii::$app->request->get('rcv_date');
    }

    $dataProvider = $searchModel->search($_GET);

Tabs::clearLocalStorage();

Url::remember();
\Yii::$app->session['__crudReturnUrl'] = null;

return $this->render('index', [
'dataProvider' => $dataProvider,
    'searchModel' => $searchModel,
]);
}

/**
* Displays a single SapPoRcv model.
* @param string $material_document_number
*
* @return mixed
*/
public function actionView($material_document_number)
{
\Yii::$app->session['__crudReturnUrl'] = Url::previous();
Url::remember();
Tabs::rememberActiveState();

return $this->render('view', [
'model' => $this->findModel($material_document_number),
]);
}

/**
* Creates a new SapPoRcv model.
* If creation is successful, the browser will be redirected to the 'view' page.
* @return mixed
*/
public function actionCreate()
{
$model = new SapPoRcv;

try {
if ($model->load($_POST) && $model->save()) {
return $this->redirect(['view', 'material_document_number' => $model->material_document_number]);
} elseif (!\Yii::$app->request->isPost) {
$model->load($_GET);
}
} catch (\Exception $e) {
$msg = (isset($e->errorInfo[2]))?$e->errorInfo[2]:$e->getMessage();
$model->addError('_exception', $msg);
}
return $this->render('create', ['model' => $model]);
}

/**
* Updates an existing SapPoRcv model.
* If update is successful, the browser will be redirected to the 'view' page.
* @param string $material_document_number
* @return mixed
*/
public function actionUpdate($material_document_number)
{
$model = $this->findModel($material_document_number);

if ($model->load($_POST) && $model->save()) {
return $this->redirect(Url::previous());
} else {
return $this->render('update', [
'model' => $model,
]);
}
}

/**
* Deletes an existing SapPoRcv model.
* If deletion is successful, the browser will be redirected to the 'index' page.
* @param string $material_document_number
* @return mixed
*/
public function actionDelete($material_document_number)
{
try {
$this->findModel($material_document_number)->delete();
} catch (\Exception $e) {
$msg = (isset($e->errorInfo[2]))?$e->errorInfo[2]:$e->getMessage();
\Yii::$app->getSession()->addFlash('error', $msg);
return $this->redirect(Url::previous());
}

// TODO: improve detection
$isPivot = strstr('$material_document_number',',');
if ($isPivot == true) {
return $this->redirect(Url::previous());
} elseif (isset(\Yii::$app->session['__crudReturnUrl']) && \Yii::$app->session['__crudReturnUrl'] != '/') {
Url::remember(null);
$url = \Yii::$app->session['__crudReturnUrl'];
\Yii::$app->session['__crudReturnUrl'] = null;

return $this->redirect($url);
} else {
return $this->redirect(['index']);
}
}

/**
* Finds the SapPoRcv model based on its primary key value.
* If the model is not found, a 404 HTTP exception will be thrown.
* @param string $material_document_number
* @return SapPoRcv the loaded model
* @throws HttpException if the model cannot be found
*/
protected function findModel($material_document_number)
{
if (($model = SapPoRcv::findOne($material_document_number)) !== null) {
return $model;
} else {
throw new HttpException(404, 'The requested page does not exist.');
}
}
}
